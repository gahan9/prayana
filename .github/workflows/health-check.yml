name: Health Check

on:
  schedule:
    # Run every 15 minutes
    - cron: "*/15 * * * *"
  workflow_dispatch:

jobs:
  health-check:
    name: Health Check – ${{ matrix.environment.name }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        environment:
          - name: Production
            url: ${{ vars.PRODUCTION_URL || 'https://prayana-app.web.app' }}
          - name: Staging
            url: ${{ vars.STAGING_URL || 'https://prayana-app--staging.web.app' }}

    steps:
      - name: Ping /api/health
        id: health
        run: |
          STATUS=$(curl -s -o /tmp/health.json -w "%{http_code}" \
            --max-time 10 \
            --retry 3 \
            --retry-delay 5 \
            "${{ matrix.environment.url }}/api/health")

          echo "http_status=$STATUS" >> "$GITHUB_OUTPUT"
          echo "body=$(cat /tmp/health.json)" >> "$GITHUB_OUTPUT"

          if [ "$STATUS" != "200" ]; then
            echo "❌ Health check failed for ${{ matrix.environment.name }} (HTTP $STATUS)"
            exit 1
          fi

          echo "✅ Health check passed for ${{ matrix.environment.name }} (HTTP $STATUS)"
          cat /tmp/health.json

      - name: Validate response schema
        if: steps.health.outputs.http_status == '200'
        run: |
          BODY='${{ steps.health.outputs.body }}'
          STATUS=$(echo "$BODY" | python3 -c "import sys,json; d=json.load(sys.stdin); print(d.get('status',''))")
          if [ "$STATUS" != "ok" ]; then
            echo "❌ Health status is not 'ok': $STATUS"
            exit 1
          fi
          echo "✅ Response schema valid"

      - name: Create GitHub Issue on failure
        if: failure()
        uses: actions/github-script@v7
        with:
          script: |
            const title = `🚨 Health check failed: ${{ matrix.environment.name }}`;
            const body = [
              '## Health Check Failure',
              '',
              `**Environment:** ${{ matrix.environment.name }}`,
              `**URL:** ${{ matrix.environment.url }}/api/health`,
              `**HTTP Status:** ${{ steps.health.outputs.http_status }}`,
              `**Run:** ${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`,
              `**Time:** ${new Date().toISOString()}`,
            ].join('\n');

            // Search for an existing open issue to avoid duplicates
            const { data: issues } = await github.rest.issues.listForRepo({
              owner: context.repo.owner,
              repo: context.repo.repo,
              state: 'open',
              labels: 'health-check,incident',
            });

            const existing = issues.find(i => i.title === title);
            if (!existing) {
              await github.rest.issues.create({
                owner: context.repo.owner,
                repo: context.repo.repo,
                title,
                body,
                labels: ['health-check', 'incident', 'bug'],
              });
            }
